import { WorkerpipePipe } from './workerpipe.pipe';

describe('WorkerpipePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkerpipePipe();
    expect(pipe).toBeTruthy();
  });
});
