import { UserManagePipe } from './user-manage.pipe';

describe('UserManagePipe', () => {
  it('create an instance', () => {
    const pipe = new UserManagePipe();
    expect(pipe).toBeTruthy();
  });
});
